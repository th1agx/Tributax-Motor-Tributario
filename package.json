{
  "name": "tributax",
  "private": true,
  "version": "0.1.0",
  "description": "Tributax — motor de decisão tributária brasileiro",
  "license": "UNLICENSED",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm run test --workspace=@tributax/domain && npm run test --workspace=@tributax/api",
    "build": "npm run build --workspace=@tributax/domain && npm run build --workspace=@tributax/api"
  },
  "engines": {
    "node": ">=22"
  }
}
