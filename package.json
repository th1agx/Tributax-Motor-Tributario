{
  "name": "tributax",
  "private": true,
  "version": "0.1.0",
  "description": "Tributax — motor de decisão tributária brasileiro",
  "license": "UNLICENSED",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "npm run start --workspace=@tributax/api",
    "test": "npm run test --workspace=@tributax/domain && npm run test --workspace=@tributax/collector && npm run test --workspace=@tributax/api && npm run test --workspace=@tributax/infrastructure",
    "build": "npm run build --workspace=@tributax/domain && npm run build --workspace=@tributax/collector && npm run build --workspace=@tributax/infrastructure && npm run build --workspace=@tributax/api"
  },
  "engines": {
    "node": ">=22"
  }
}
